{namespace <%= blockNamespace %>.Template}

/**
 * Template for <%= blockNamespace %> block
 *
 * @param? params {{
 *     ?data: {
 *     }
 * }}
 * @constructor
 * @extends cl.iControl.Template
 */
{template .<%= blockName %>}
    {call .init data="all" /}
{/template}

/**
 * @override
 */
{template .rootClass}

{/template}

/**
 * @override
 */
{template .body}

{/template}
