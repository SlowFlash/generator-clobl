{namespace <%= soyBlockProvide %>}

/**
 * Template for Pablo <%= blockDescriptionName %> block
 *
 * @param? params {{
 *     ?data: {
 *     }
 * }}
 * @constructor
 * @extends cl.iControl.Template
 */
{template .<%= soyConstructorTemplateName %>}
    {call .init data="all" /}
{/template}

/**
 * @override
 */
{template .rootClass}
    <%= name %>
{/template}

/**
 * @override
 */
{template .body}

{/template}

